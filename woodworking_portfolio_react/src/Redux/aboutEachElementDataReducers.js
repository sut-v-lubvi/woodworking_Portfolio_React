// import { sliderStairsDataArray } from "./sliderData";

// export const addDataSingleCreator = (id) => {
//   return { type: "ADD", id };
// };

// let initialState = {
//   sliderData: {},
// };

// let aboutEachElementDataReducers = (state = initialState, action) => {
//   switch (action.type) {
//     case "ADD": {
//       console.log(action);
//       let newState = { ...state };
//       newState.sliderData = { ...state.sliderData };
//       newState.sliderData = sliderStairsDataArray[action.id];
//       return newState;
//     }
//     default:
//       return state;
//   }
// };

// export default aboutEachElementDataReducers;
